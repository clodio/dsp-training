name: Docker Image CI

on:
  push:
    branches: [ "exercice4-solution" ]
  pull_request:
    branches: [ "exercice4-solution" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag exo5:$(date +%s)
